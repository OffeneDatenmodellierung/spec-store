# Agent Context — spec-store [generated 2026-04-07T07:00Z]

> Auto-generated by spec-store. Regenerate with: `spec-store context generate`

## Your Scope

No worktree claimed. Claim one before starting:
```bash
spec-store worktree claim <your-branch> --contract <interface-file> --owner <agent-id>
```

## How to Query the Store

```bash
spec-store search "<natural language query>"   # find existing functions
spec-store decision list                        # see all ADRs
spec-store worktree list                        # see who owns what
spec-store coverage report                      # per-file coverage
spec-store quality check --path src/            # quality gate check
spec-store status                               # overall health
```

## Before Writing Any Code

Run `spec-store search "<intent>"` first.
At similarity ≥ 0.85 → investigate the existing function.
At similarity ≥ 0.95 → you are blocked; the function effectively already exists.

## Quality Rules

Every file you touch must pass:
- **300** max code lines per file
- **50** max lines per function
- **15** max functions per file
- **10** max cyclomatic complexity
- **85%** min test coverage per file

## Active Architectural Decisions

- [2026-04-07] Word-bag local embeddings by default; no API key needed for search
- [2026-04-07] Baselines ratchet upward only — coverage cannot regress
- [2026-04-07] Per-file coverage threshold, not project-wide
- [2026-04-07] Git hooks committed to `.githooks/`; all devs get them on clone + init
- [2026-04-07] `NoneProvider` keeps CLI functional when no AI key is configured
